package com.davispcook.asymmetrik.executable;

/**
 * Enum used by the Main class to
 * keep track of it's execution state, i.e.
 * it functions as a state machine and this
 * enum represents the possible states
 * 
 * @author Davis
 *
 */
public enum ExecutionMode {
	TRAINING, TESTING
}
